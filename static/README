This folder contains all templates and static files for jobGeras.

***     ./locale/ folder contains language catalogs in .js files

***     ./fb/ folder contains xd_receiver.htm, a file to allow cross domain communication between jobGears and Facebook

***     ./images/ folder contains all the images needed for the Application

***     ./css/ contains .css files

***     ./js/ contains .js files.
            - homeutils.js implements all the effects and function utilities in the application. 
            - home.js is the main javascript file. save(), erase(), cancel(), edit() functions are in this file.
            - homefb.js is the facebook API support file.
            - homeform.js contains the newForm AJAX requests. Is this file that makes possible to add new forms to the CV.
            - hometwitter.js is the twitter API support file. 

***     ./form/ folder cotains form templates
            ./form/render/ are those ones rendered after a user request
            ./form/init/ are those rendered at the begining of a user session


***     ./s/ folder to store all static CV's generated by the users
